<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">


    <ImageView
        android:id="@+id/promoCodeBack"
        android:layout_width="match_parent"
        android:layout_height="0dp"

        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/back_for_promo_code" />

    <TextView
        android:id="@+id/promoCodeTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="12dp"
        android:fontFamily="@font/sfpro_regular"
        android:includeFontPadding="false"
        android:text="@string/promo_code_view_text"
        android:textSize="14sp"
        android:textColor="#000"
        app:layout_constraintBottom_toBottomOf="@+id/promoCodeBack"
        app:layout_constraintStart_toStartOf="@+id/promoCodeBack"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/promoCodePromoText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="8dp"
        android:layout_marginBottom="12dp"
        android:fontFamily="@font/sfpro_regular"
        android:includeFontPadding="false"
        android:text="Some code"
        android:textSize="14sp"
        android:textColor="#000"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/promoCodeButCopy"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="1.0" />

    <ImageView
        android:id="@+id/promoCodeButCopy"
        android:layout_width="wrap_content"
        android:layout_height="15dp"
        android:layout_marginEnd="12dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/promoCodeBack"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/copy_icon" />
</androidx.constraintlayout.widget.ConstraintLayout>